########################################################################################

ENV = production
TRANSPORT = ssh
.PHONY = all release

########################################################################################

all: release

release:
	ansible-playbook deploy.yml -i environments/$(ENV) \
		--extra-vars="env=$(ENV)" \
		-c $(TRANSPORT) 

########################################################################################
